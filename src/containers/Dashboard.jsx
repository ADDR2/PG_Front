import React from 'react';
import { connect } from 'react-redux';

const Dashboard = () => {
    return (
        <div></div>
    );
};

const mS = () => ({});
const mD = {};

export default connect(mS, mD)(Dashboard);